# omac font registry entry. `family` is the exact name apps reference; `cask` is
# the Homebrew cask that installs it (see software/groups/fonts.Brewfile).
family = "Maple Mono NF"
cask = "font-maple-mono-nf"
