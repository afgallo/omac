# Default credentials + host ports for the omac dev stack. These are local-dev
# defaults, not secrets — change them in the deployed copy at
# ~/.config/omac/services/.env (omac never overwrites your edits).
POSTGRES_USER=omac
POSTGRES_PASSWORD=omac
POSTGRES_DB=omac
POSTGRES_PORT=5432
REDIS_PORT=6379
